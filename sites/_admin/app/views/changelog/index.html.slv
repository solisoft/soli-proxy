<!-- Header -->
<div class="mb-6">
    <p class="text-sm text-surface-400">Release history for soli-proxy. Mirrors <span class="font-mono text-surface-300">CHANGELOG.md</span> in the repository.</p>
</div>

<!-- 0.34.0 -->
<div class="bg-surface-900/60 border border-white/5 rounded-xl overflow-hidden mb-5">
    <div class="px-5 py-4 border-b border-white/5 flex items-baseline justify-between gap-3 flex-wrap">
        <div class="flex items-baseline gap-3">
            <h3 class="text-base font-semibold text-white font-mono">v0.34.0</h3>
            <span class="px-2 py-0.5 rounded-full text-xs font-medium bg-accent-500/10 text-accent-400 border border-accent-500/20">latest</span>
        </div>
        <span class="text-xs text-surface-500">2026-09-15</span>
    </div>

    <ul class="divide-y divide-white/5">
        <li class="px-5 py-4 flex gap-3">
            <span class="shrink-0 mt-0.5 px-2 py-0.5 rounded text-xs font-semibold bg-accent-500/10 text-accent-400 border border-accent-500/20">feature</span>
            <div class="min-w-0">
                <div class="text-sm font-medium text-white">Scale to zero</div>
                <p class="text-sm text-surface-400 mt-1"><span class="font-mono text-surface-300">idle_timeout = 900</span> in <span class="font-mono text-surface-300">app.infos</span> stops an app after fifteen quiet minutes and starts it again on the next request, which is held while it boots &mdash; about a second for the first visitor, nothing for everyone after. The stop goes through the same path as <span class="font-mono text-surface-300">soli-proxy stop</span>, so it is not read as a crash: no failover, no quarantine. A sleeping app keeps its certificate and keeps winning over static rules for its domains.</p>
                <p class="text-sm text-surface-400 mt-2">The default is <span class="font-mono text-surface-300">0</span> &mdash; never sleep &mdash; and that is the right value for anything with cron jobs, background workers or WebSocket rooms. <span class="font-mono text-surface-300">[apps] idle_timeout</span> in <span class="font-mono text-surface-300">config.toml</span> sets a fleet-wide default; <span class="font-mono text-surface-300">_admin</span> never sleeps.</p>
            </div>
        </li>

        <li class="px-5 py-4 flex gap-3">
            <span class="shrink-0 mt-0.5 px-2 py-0.5 rounded text-xs font-semibold bg-accent-500/10 text-accent-400 border border-accent-500/20">feature</span>
            <div class="min-w-0">
                <div class="text-sm font-medium text-white">Per-environment sections in app.infos</div>
                <p class="text-sm text-surface-400 mt-1">A manifest may carry <span class="font-mono text-surface-300">[development]</span> and <span class="font-mono text-surface-300">[production]</span> sections, and <span class="font-mono text-surface-300">--dev</span> picks one &mdash; the same flag that already appends <span class="font-mono text-surface-300">--dev</span> to an auto-detected Soli start script and registers each app's <span class="font-mono text-surface-300">.test</span> alias. One file instead of a dev copy and a prod copy that nobody diffs.</p>
                <p class="text-sm text-surface-400 mt-2">Applied key by key over the top level, with nested tables merging rather than replacing, so <span class="font-mono text-surface-300">[production.auth.users]</span> adds accounts without discarding the <span class="font-mono text-surface-300">noauth</span> list. The unselected section is dropped unread. A manifest with neither parses exactly as before.</p>
            </div>
        </li>

        <li class="px-5 py-4 flex gap-3">
            <span class="shrink-0 mt-0.5 px-2 py-0.5 rounded text-xs font-semibold bg-cyan-500/10 text-cyan-400 border border-cyan-500/20">fix</span>
            <div class="min-w-0">
                <div class="text-sm font-medium text-white">Unknown app.infos keys are logged</div>
                <p class="text-sm text-surface-400 mt-1">An unrecognised key was dropped silently &mdash; <span class="font-mono text-surface-300">worker = 4</span> ran the app with one worker and said nothing. Startup now names it. Still ignored rather than fatal: refusing the manifest would take a running app off the routing table over a typo.</p>
            </div>
        </li>
    </ul>
</div>

<!-- 0.33.0 -->
<div class="bg-surface-900/60 border border-white/5 rounded-xl overflow-hidden mb-5">
    <div class="px-5 py-4 border-b border-white/5 flex items-baseline justify-between gap-3 flex-wrap">
        <div class="flex items-baseline gap-3">
            <h3 class="text-base font-semibold text-white font-mono">v0.33.0</h3>
        </div>
        <span class="text-xs text-surface-500">2026-09-10</span>
    </div>

    <ul class="divide-y divide-white/5">
        <li class="px-5 py-4 flex gap-3">
            <span class="shrink-0 mt-0.5 px-2 py-0.5 rounded text-xs font-semibold bg-accent-500/10 text-accent-400 border border-accent-500/20">feature</span>
            <div class="min-w-0">
                <div class="text-sm font-medium text-white">Basic Auth per app</div>
                <p class="text-sm text-surface-400 mt-1">An app declares its own credentials in <span class="font-mono text-surface-300">app.infos</span>: an <span class="font-mono text-surface-300">[auth.users]</span> table of <span class="font-mono text-surface-300">username = "bcrypt hash"</span> entries, plus <span class="font-mono text-surface-300">[auth] noauth</span> for paths served without one. Route <span class="font-mono text-surface-300">@auth</span> could never cover an app, since static rules for app-managed domains are pruned. Covers derived domains and aliases, and WebSocket upgrades. Protected apps carry an <span class="font-mono text-surface-300">auth</span> badge on the Apps page; the API returns usernames, never hashes.</p>
            </div>
        </li>

        <li class="px-5 py-4 flex gap-3">
            <span class="shrink-0 mt-0.5 px-2 py-0.5 rounded text-xs font-semibold bg-cyan-500/10 text-cyan-400 border border-cyan-500/20">fix</span>
            <div class="min-w-0">
                <div class="text-sm font-medium text-white">A contested domain resolves to one app</div>
                <p class="text-sm text-surface-400 mt-1">The host &rarr; app lookup iterated a hash map, so two apps declaring one domain could give a different answer than the routing table (which picks by name order) &mdash; misattributing per-app metrics, and with per-app auth, potentially applying the wrong app's credentials.</p>
            </div>
        </li>
    </ul>
</div>

<!-- 0.32.0 -->
<div class="bg-surface-900/60 border border-white/5 rounded-xl overflow-hidden mb-5">
    <div class="px-5 py-4 border-b border-white/5 flex items-baseline justify-between gap-3 flex-wrap">
        <div class="flex items-baseline gap-3">
            <h3 class="text-base font-semibold text-white font-mono">v0.32.0</h3>
        </div>
        <span class="text-xs text-surface-500">2026-09-09</span>
    </div>

    <ul class="divide-y divide-white/5">
        <li class="px-5 py-4 flex gap-3">
            <span class="shrink-0 mt-0.5 px-2 py-0.5 rounded text-xs font-semibold bg-accent-500/10 text-accent-400 border border-accent-500/20">feature</span>
            <div class="min-w-0">
                <div class="text-sm font-medium text-white">Exempt paths from a route's Basic Auth</div>
                <p class="text-sm text-surface-400 mt-1">A whole domain can be password-protected while the endpoints machines call &mdash; a payment webhook, a health probe &mdash; stay reachable without credentials: <span class="font-mono text-surface-300">@auth:admin:$2b$12$... @noauth:/webhooks/stripe,/hooks/*</span>. Each entry is an exact path or a prefix ending in <span class="font-mono text-surface-300">*</span>, matched against the URL as the client sent it. Editable here under Routes &rarr; Basic Auth, and exposed as <span class="font-mono text-surface-300">auth_exempt</span> on the admin API.</p>
                <p class="text-sm text-surface-400 mt-2">The match fails closed: a path carrying percent-encoding or a <span class="font-mono text-surface-300">..</span> segment is never exempt, so a request cannot walk out of the carve-out into a protected path that a normalising backend would resolve differently.</p>
            </div>
        </li>
    </ul>
</div>

<!-- 0.31.0 -->
<div class="bg-surface-900/60 border border-white/5 rounded-xl overflow-hidden mb-5">
    <div class="px-5 py-4 border-b border-white/5 flex items-baseline justify-between gap-3 flex-wrap">
        <div class="flex items-baseline gap-3">
            <h3 class="text-base font-semibold text-white font-mono">v0.31.0</h3>
        </div>
        <span class="text-xs text-surface-500">2026-09-04</span>
    </div>

    <ul class="divide-y divide-white/5">
        <li class="px-5 py-4 flex gap-3">
            <span class="shrink-0 mt-0.5 px-2 py-0.5 rounded text-xs font-semibold bg-rose-500/10 text-rose-400 border border-rose-500/20">security</span>
            <div class="min-w-0">
                <div class="text-sm font-medium text-white">Path traversal rejected before routing</div>
                <p class="text-sm text-surface-400 mt-1">Rules match on the raw path and per-route auth binds to the matched rule, so <span class="font-mono text-surface-300">/api/../admin/users</span> could pass an open <span class="font-mono text-surface-300">/api/</span> rule and land on a protected route at any backend that normalises. Dot segments in every spelling (literal, <span class="font-mono text-surface-300">%2e</span>, <span class="font-mono text-surface-300">..;</span>, <span class="font-mono text-surface-300">..\</span>) and encoded slashes are answered 400. Backends that carry <span class="font-mono text-surface-300">%2F</span> as data can set <span class="font-mono text-surface-300">[server] allow_encoded_slash = true</span>.</p>
            </div>
        </li>

        <li class="px-5 py-4 flex gap-3">
            <span class="shrink-0 mt-0.5 px-2 py-0.5 rounded text-xs font-semibold bg-rose-500/10 text-rose-400 border border-rose-500/20">security</span>
            <div class="min-w-0">
                <div class="text-sm font-medium text-white">Multi-tenant mode for untrusted apps</div>
                <p class="text-sm text-surface-400 mt-1"><span class="font-mono text-surface-300">[apps] multi_tenant = true</span> treats every app as untrusted code: a <span class="font-mono text-surface-300">docker_image</span> is mandatory, every container gets a read-only root, dropped capabilities, no-new-privileges, pid/memory/cpu ceilings and a non-root uid that tenant config cannot weaken, and <span class="font-mono text-surface-300">docker_options</span> is validated against an allowlist. Bind mounts may only be the app's own site directory, passed to docker by its canonical path. <span class="font-mono text-surface-300">docker_network</span> may not join the host or another container in any mode, and the single-tenant denylist now reads docker's syntax (<span class="font-mono text-surface-300">-v/:/host</span>, <span class="font-mono text-surface-300">--mount type=bind,source=/</span>).</p>
            </div>
        </li>

        <li class="px-5 py-4 flex gap-3">
            <span class="shrink-0 mt-0.5 px-2 py-0.5 rounded text-xs font-semibold bg-rose-500/10 text-rose-400 border border-rose-500/20">security</span>
            <div class="min-w-0">
                <div class="text-sm font-medium text-white">Admin API hardening</div>
                <p class="text-sm text-surface-400 mt-1">Mutating requests without <span class="font-mono text-surface-300">X-Api-Key</span> must carry an <span class="font-mono text-surface-300">X-Requested-With</span> header, which an HTML form cannot set. Route password hashes are never returned; a re-submitted entry with an empty hash keeps the existing one, matched by the rule's matcher rather than its position so a whole-table update that deletes or reorders rules cannot hand a route another route's password. Empty admin credentials (<span class="font-mono text-surface-300">api_key = ""</span>) count as unset. Lua hook errors fail closed with a 500.</p>
            </div>
        </li>

        <li class="px-5 py-4 flex gap-3">
            <span class="shrink-0 mt-0.5 px-2 py-0.5 rounded text-xs font-semibold bg-cyan-500/10 text-cyan-400 border border-cyan-500/20">fix</span>
            <div class="min-w-0">
                <div class="text-sm font-medium text-white">Apps get their own HOME and an environment allowlist</div>
                <p class="text-sm text-surface-400 mt-1">The proxy drops privileges to the app's user but handed the child its own <span class="font-mono text-surface-300">HOME</span> (<span class="font-mono text-surface-300">/root</span> under systemd), silently breaking soli's package cache, registry credentials and the Tailwind CLI. It now comes from the passwd entry of the user the app runs as. A short allowlist (<span class="font-mono text-surface-300">HTTP(S)_PROXY</span>, <span class="font-mono text-surface-300">SSL_CERT_FILE</span>, <span class="font-mono text-surface-300">XDG_CACHE_HOME</span>, the soli toolchain variables) survives the cleared environment, for native and Docker apps alike.</p>
            </div>
        </li>
    </ul>
</div>

<!-- 0.29.1 -->
<div class="bg-surface-900/60 border border-white/5 rounded-xl overflow-hidden mb-5">
    <div class="px-5 py-4 border-b border-white/5 flex items-baseline justify-between gap-3 flex-wrap">
        <div class="flex items-baseline gap-3">
            <h3 class="text-base font-semibold text-white font-mono">v0.29.1</h3>
        </div>
        <span class="text-xs text-surface-500">2026-07-28</span>
    </div>

    <ul class="divide-y divide-white/5">
        <li class="px-5 py-4 flex gap-3">
            <span class="shrink-0 mt-0.5 px-2 py-0.5 rounded text-xs font-semibold bg-emerald-500/10 text-emerald-400 border border-emerald-500/20">feat</span>
            <div class="min-w-0">
                <div class="text-sm font-medium text-white">Deploy trigger file</div>
                <p class="text-sm text-surface-400 mt-1">Touching <span class="font-mono text-surface-300">restart.txt</span> at the root of a site triggers a zero-downtime blue/green deploy, so a deploy script can end with <span class="font-mono text-surface-300">touch &lt;site&gt;/restart.txt</span> instead of an SSH-side restart. The file is polled (2s by default) rather than watched, because inotify does not traverse the symlinks that sites usually are. The first poll after startup only records a baseline, so an existing trigger file never redeploys everything on daemon restart. Configurable via <span class="font-mono text-surface-300">[apps].restart_trigger_file</span> and <span class="font-mono text-surface-300">restart_trigger_poll_secs</span> (<span class="font-mono text-surface-300">0</span> disables it).</p>
            </div>
        </li>

        <li class="px-5 py-4 flex gap-3">
            <span class="shrink-0 mt-0.5 px-2 py-0.5 rounded text-xs font-semibold bg-rose-500/10 text-rose-400 border border-rose-500/20">security</span>
            <div class="min-w-0">
                <div class="text-sm font-medium text-white">Quarantine on failed start</div>
                <p class="text-sm text-surface-400 mt-1">An app that fails to start is no longer restarted forever. Previously the health loop called failover every 30s on any unreachable app with no failure cap, leaving a broken deploy flapping indefinitely. The failed slot is killed and marked <span class="font-mono text-surface-300">Failed</span>, the previous slot keeps serving, and all automatic remediation skips the app until an explicit deploy. Exposed as <span class="font-mono text-surface-300">"quarantined": true</span> on the apps API plus an SSE event.</p>
            </div>
        </li>

        <li class="px-5 py-4 flex gap-3">
            <span class="shrink-0 mt-0.5 px-2 py-0.5 rounded text-xs font-semibold bg-cyan-500/10 text-cyan-400 border border-cyan-500/20">fix</span>
            <div class="min-w-0">
                <div class="text-sm font-medium text-white">systemd unit and Docker entrypoint repaired</div>
                <p class="text-sm text-surface-400 mt-1">Both shipped deployment paths could never start. The systemd unit invoked a <span class="font-mono text-surface-300">daemon</span> subcommand that does not exist and hardcoded a developer's home directory; the Docker <span class="font-mono text-surface-300">CMD</span> passed <span class="font-mono text-surface-300">--config</span>, which is not a flag. Both now use <span class="font-mono text-surface-300">--conf</span> (which takes <span class="font-mono text-surface-300">proxy.conf</span>, not <span class="font-mono text-surface-300">config.toml</span>) plus <span class="font-mono text-surface-300">--sites-dir</span>, and set a working directory so <span class="font-mono text-surface-300">run/</span> and <span class="font-mono text-surface-300">certs/</span> resolve to the mounted volumes.</p>
            </div>
        </li>
    </ul>
</div>

<!-- 0.29.0 -->
<div class="bg-surface-900/60 border border-white/5 rounded-xl overflow-hidden mb-5">
    <div class="px-5 py-4 border-b border-white/5 flex items-baseline justify-between gap-3 flex-wrap">
        <h3 class="text-base font-semibold text-white font-mono">v0.29.0</h3>
        <span class="text-xs text-surface-500">Security hardening</span>
    </div>

    <ul class="divide-y divide-white/5">
        <li class="px-5 py-4 flex gap-3">
            <span class="shrink-0 mt-0.5 px-2 py-0.5 rounded text-xs font-semibold bg-rose-500/10 text-rose-400 border border-rose-500/20">security</span>
            <div class="min-w-0">
                <div class="text-sm font-medium text-white">Streaming request-body limits</div>
                <p class="text-sm text-surface-400 mt-1">Body-size limits are enforced on chunked and HTTP/2 bodies by streaming through a limiter and returning <span class="font-mono text-surface-300">413</span>, instead of blanket-rejecting chunked encoding. A body omitting or lying about <span class="font-mono text-surface-300">Content-Length</span> can no longer buffer without bound.</p>
            </div>
        </li>

        <li class="px-5 py-4 flex gap-3">
            <span class="shrink-0 mt-0.5 px-2 py-0.5 rounded text-xs font-semibold bg-rose-500/10 text-rose-400 border border-rose-500/20">security</span>
            <div class="min-w-0">
                <div class="text-sm font-medium text-white">Header and target validation</div>
                <p class="text-sm text-surface-400 mt-1">WebSocket upgrade requests and forwarded headers are guarded against CR/LF injection; proxy target URLs from config and Lua overrides are restricted to <span class="font-mono text-surface-300">http</span>, <span class="font-mono text-surface-300">https</span>, and <span class="font-mono text-surface-300">redirect</span> schemes, closing an SSRF avenue.</p>
            </div>
        </li>

        <li class="px-5 py-4 flex gap-3">
            <span class="shrink-0 mt-0.5 px-2 py-0.5 rounded text-xs font-semibold bg-rose-500/10 text-rose-400 border border-rose-500/20">security</span>
            <div class="min-w-0">
                <div class="text-sm font-medium text-white">Admin API hardening</div>
                <p class="text-sm text-surface-400 mt-1">A loud warning when the admin API binds loopback without authentication; plaintext <span class="font-mono text-surface-300">ADMIN_PASSWORD</span> is bcrypt-hashed at startup instead of stored verbatim; deployment-log responses capped at 256 KiB and request bodies at 1 MB.</p>
            </div>
        </li>

        <li class="px-5 py-4 flex gap-3">
            <span class="shrink-0 mt-0.5 px-2 py-0.5 rounded text-xs font-semibold bg-purple-500/10 text-purple-400 border border-purple-500/20">perf</span>
            <div class="min-w-0">
                <div class="text-sm font-medium text-white">Shared connection pool and faster routing</div>
                <p class="text-sm text-surface-400 mt-1">All listeners share one connection pool with a per-host idle cap of 64. Host matching is a single case-insensitive scan again, replacing an "optimization" that rebuilt a throwaway hash map on every request.</p>
            </div>
        </li>
    </ul>
</div>

<p class="text-xs text-surface-500">
    Full history in <span class="font-mono text-surface-400">CHANGELOG.md</span>.
</p>
